const HIGH_TEMPERATURES = {
  yesterday: 75,
  today: 77,
  tomorrow: 80
};

// Sintaxis de desestructuración

// Asignación de valores directamente desde un objeto
const {today ,tomorrow} = HIGH_TEMPERATURES 